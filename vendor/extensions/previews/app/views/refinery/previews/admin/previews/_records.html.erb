<%= render 'refinery/admin/search_header', :url => refinery.previews_admin_previews_path %>
<div class='pagination_container'>
  <% if @previews.any? %>
    <%= render 'previews' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
