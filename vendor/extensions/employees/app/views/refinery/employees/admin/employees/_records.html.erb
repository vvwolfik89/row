<%= render 'refinery/admin/search_header', :url => refinery.employees_admin_employees_path %>
<div class='pagination_container'>
  <% if @employees.any? %>
    <%= render 'employees' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
