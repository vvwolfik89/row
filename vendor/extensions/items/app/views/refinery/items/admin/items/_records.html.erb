<%= render 'refinery/admin/search_header', :url => refinery.items_admin_items_path %>
<div class='pagination_container'>
  <% if @items.any? %>
    <%= render 'items' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
